[project]
name = "learning-pybricks"
version = "0.1.0"
description = "Agentic coding eval platform with LEGO Inventor Hub, iOS BLE bridge, and WebSocket server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "websockets>=13.0",
    "zeroconf>=0.128.0",
]

[dependency-groups]
dev = [
    "pybricksdev",
]
